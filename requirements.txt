# Default requirements: the Streamlit dashboard (deployed on Streamlit Community Cloud).
# Streamlit Cloud always installs THIS file, so it must stay slim and conflict-free.
# Reads everything from MongoDB; never runs live inference or training.
# Excludes tensorflow/optuna/mlflow/jupyter (training-only) and dash/gunicorn (replaced by Streamlit).
# Full training pipeline deps live in requirements-training.txt.

# Dashboard framework
streamlit>=1.40.0
plotly>=5.19.0

# Data + utilities
pandas>=2.1.0
numpy>=1.26.0
python-dotenv>=1.0.0

# Database
pymongo[srv]>=4.6.0

# ML — needed to unpickle champion artifacts and compute SHAP / intervals
scikit-learn>=1.4.0
xgboost>=2.0.0
catboost>=1.2.0
lightgbm>=4.3.0
shap>=0.44.0
mapie>=0.8.0

# Time-series decomposition (build_feature_row → STL, imported transitively)
statsmodels>=0.14.0
