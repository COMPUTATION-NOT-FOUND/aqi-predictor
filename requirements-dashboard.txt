# Dashboard requirements for Streamlit Community Cloud.
# Reads everything from MongoDB; never runs live inference or training.
# Excludes tensorflow/optuna (training-only) and dash/gunicorn (replaced by Streamlit).

# Dashboard framework
streamlit>=1.40.0
plotly>=5.19.0

# Data + utilities
pandas>=2.1.0
numpy>=1.26.0
python-dotenv>=1.0.0

# Database
pymongo[srv]>=4.6.0

# ML — needed to unpickle champion artifacts and compute SHAP / intervals
scikit-learn>=1.4.0
xgboost>=2.0.0
catboost>=1.2.0
lightgbm>=4.3.0
shap>=0.44.0
mapie>=0.8.0

# Time-series decomposition (build_feature_row → STL, imported transitively)
statsmodels>=0.14.0
