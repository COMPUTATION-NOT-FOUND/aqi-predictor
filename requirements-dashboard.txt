# Dashboard-only requirements for Render deployment
# Excludes tensorflow, optuna, mrmr-selection (training-only)

# Server
gunicorn>=21.2.0

# Data fetching & utilities
openmeteo-requests>=0.3.0
requests-cache>=1.2.0
retry-requests>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.1.0
numpy>=1.26.0

# Feature store
hopsworks[python]==4.7.*

# Experiment tracking (reads mlruns/ for ablation tab)
mlflow>=2.11.0

# ML — needed to unpickle champion models loaded from Hopsworks
scikit-learn>=1.4.0
xgboost>=2.0.0
catboost>=1.2.0
lightgbm>=4.3.0

# Explainability
shap>=0.44.0

# Conformal prediction
mapie>=0.8.0

# Time series decomposition
statsmodels>=0.14.0

# Dashboard
plotly>=5.19.0
dash>=2.16.0
dash-bootstrap-components>=1.5.0
